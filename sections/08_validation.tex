\section{模型检验与敏感性分析}

模型检验应覆盖数据、计算和结论三个层面：首先用留出样本或交叉验证评估外推误差；其次使用独立实现或极端输入核对程序；最后比较不同合理参数、标准化方式和情景假设下的主要结论。

对本示例而言，可将每个权重在基准值附近扰动 $\pm 10\%$，归一化后重新计算排名，并用问题三程序汇总各对象的最小、最大得分。若结论发生变化，应报告临界参数和受影响对象，而不应只展示稳定场景。
