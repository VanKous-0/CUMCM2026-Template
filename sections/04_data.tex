\section{数据预处理}

\subsection{数据质量检查}
% 说明样本量、字段类型、重复值、取值范围及数据来源。
在此汇总数据规模和质量，并给出可复现的检查规则。

\subsection{缺失值处理}
% 说明缺失比例、缺失机制及删除/插补方法；避免只写“删除缺失值”。
在此说明缺失值识别、处理方法及其对结果的影响。

\subsection{异常值检测}
% 可选用箱线图、3σ、IQR、孤立森林等方法，并结合业务含义复核。
在此说明异常值判据、人工复核过程和最终保留策略。

\subsection{数据标准化}
% 根据模型选择 Z-score、极差标准化、对数变换或稳健缩放。
以效益型指标为例，可写为
\begin{equation}
  z_{ij}=\frac{x_{ij}-\min_i x_{ij}}{\max_i x_{ij}-\min_i x_{ij}}.
  \label{eq:normalization}
\end{equation}
% 极差为零时应在代码中单独处理，并在正文说明。

\subsection{特征工程与探索性分析}
% 可报告相关性、共线性、类别编码、时间特征、滞后特征和特征筛选。
在此说明特征构造与筛选依据。根据题型，可进一步选择回归、分类、聚类、时间序列或机器学习模型。
